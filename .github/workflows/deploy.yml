name: Deploy to IIS

on:
  workflow_dispatch:

jobs:
  deploy:

    runs-on: self-hosted

    steps:
    - name: Download a Build Artifact
      uses: actions/download-artifact@v3.1.0
      with:
        name: website
      

