name: Deploy to IIS

on:
  workflow_dispatch:

jobs:
  deploy:

    runs-on: self-hosted

    steps:
    - name: Download a Build Artifact      
      uses: actions/download-artifact@v4
     with:
      name: website

      

